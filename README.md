```bash
$ get get github.com/smeets/mobigen
$ $GOPATH/bin/mobigen -h
Usage of mobigen:
  -end int
        end chapter id (default -1)
  -out string
        output filename (default "book.mobi")
  -path string
        base path to root (default "~/")
  -start int
        start chapter id
```
